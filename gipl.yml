# Build GIPL from source on Linux, macOS, and Windows.
name: gipl
channels:
  - conda-forge
dependencies:
  - python =3
  # - cxx-compiler  # [win]
  - fortran-compiler
  - pkg-config
  - cmake
  - make
  - bmi-fortran =1.2.1
