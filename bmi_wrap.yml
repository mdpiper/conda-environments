# Create a PyMT component from a BMI-wrapped model.
name: wrap
channels:
  - defaults
  - csdms-stack
  - conda-forge
dependencies:
- python=3.6
- numpy
- model_metadata
- pyyaml
- jinja2
- cookiecutter
- six
- bmi_wrap=0.1.3 # supplies bmi-wrap & bmi-render
- versioneer
