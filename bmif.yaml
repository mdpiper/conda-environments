# Build the Fortran BMI bindings and example
name: bmif
channels:
  - conda-forge
dependencies:
  - python =3
  - make
  - cmake
  - fortran-compiler
  # - cxx-compiler  # [win]
  # - vs2019_win-64  # [win]
